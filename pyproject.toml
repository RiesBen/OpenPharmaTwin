[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "open-pharma-twin"
version = "0.1.0"
description = "Open-source digital twin framework for pharma plants"
requires-python = ">=3.10"
dependencies = []
